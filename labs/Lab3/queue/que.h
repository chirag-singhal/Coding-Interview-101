#include "node.c"

extern QUEUE newQ();

extern int isEmpty(QUEUE q);

extern QUEUE delQ(QUEUE q);

extern ELEMENT front(QUEUE q);

extern QUEUE addQ(QUEUE q, ELEMENT e);

extern int lengthQ(QUEUE q);
